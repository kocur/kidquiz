{
  "db": {
    "name": "db",
    "connector": "memory"
  },
  "kidquiz": {
    "host": "${DB_HOST}",
    "port": "${DB_PORT}",
    "url": "${DB_URL}",
    "database": "${DB_NAME}",
    "password": "${DB_PASSWORD}",
    "name": "${DB_NAME}",
    "user": "${DB_USER}",
    "connector": "mongodb"
  }
}
